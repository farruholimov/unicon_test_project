#### Start Command
`npm run start`

#### Prerequisites
- Set environment variables
- Create production databse and developement database
- Run `npm run updatedb` first before running `npm run dev` for the first time
